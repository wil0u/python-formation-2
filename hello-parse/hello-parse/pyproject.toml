[project]
name = "hello-parse"
version = "0.1.0"
description = "Hello World CLI avec argparse"
readme = "README.md"
requires-python = ">=3.9"

authors = [
    { name = "Ton Nom" }
]

dependencies = []

[project.scripts]
hello = "hello_parse.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"