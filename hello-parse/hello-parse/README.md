# hello-parse

CLI Python minimale utilisant `argparse`.

## Installation

```bash
pip install .
hello
hello --name Alice
hello --help```

src/
└── hello_parse/
    └── cli.py

---

### Pourquoi ce README est “bien” ✅
- clair en 10 secondes
- aucun jargon
- aucun doublon avec le code
- suffisant pour toi *et* pour un autre dev
- prêt à grandir plus tard

Quand ton projet évoluera, tu pourras **ajouter**, pas réécrire.

Si tu veux, prochaine étape naturelle :
- 1 phrase en plus “Pourquoi ce projet”
- ou une section “Développement”

Mais pour commencer : **c’est exactement ce qu’il faut** 👌